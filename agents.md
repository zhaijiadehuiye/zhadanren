# agents.md —— Q 版炸弹人 项目说明书

> 本文件是本仓库的 **AI 项目说明书**。任何 AI 编程助手（Codex / Claude Code / Trae 等）在动手改代码之前，
> 都必须先读完本文件，并**无条件遵守第 4 节的两条强制规则**。

---

## 1. 项目概述

- **目标**：复刻 4399 平台上经典的 **Q 版炸弹人** 玩法 —— **单人闯关**：炸墙开路 → 拿强化
  → 躲敌人与自己的炸弹 → 清光敌人 → 找藏在软砖下的出口 → 进入下一关，共 5 关。
- **形态**：Godot 4 项目，可本地运行，也可导出 Web 版本。
- **玩法参考**：`【番外篇】盗版炸弹人？还记得曾经火爆4399的Q版泡泡堂吗？？.flv`
  与 Flash 版《可爱炸弹人2》（`https://oldswf.com/game/97791`），已整理成可实现的条目，见第 6 节。
- **当前阶段**：5 关单人闯关可玩。core 纯逻辑层 420 项断言全绿；视图层已接入真实像素美术
  （角色 4 方向行走/死亡逐帧动画、6 种怪物、爆炸火焰帧动画、道具、瓦片）与 AI 生成插画
  （标题画面、4 角色选人立绘、地图地板纹理），并实现像素级平滑移动、仿参考游戏的左侧 HUD，
  以及一整套原创 chiptune BGM / 音效与「有重量」的打击手感（屏幕震动、打击停顿、输入缓冲、
  飘分与连锁奖励）。

## 2. 技术栈

| 项 | 选型 |
| --- | --- |
| 引擎 | Godot 4.3（stable），渲染后端 `gl_compatibility` |
| 语言 | GDScript 2.0 |
| 测试 | 仓库自带轻量单元测试框架（`tests/`），headless 运行，**无第三方依赖** |
| 版本控制 | git，远端 `https://github.com/zhaijiadehuiye/zhadanren` |

## 3. 目录结构与架构约定

```
zhadanren/
├── agents.md              # 本文件：AI 项目说明书（含强制规则）
├── project.godot          # Godot 工程配置
├── icon.svg
├── scenes/
│   └── main.tscn          # 唯一场景：挂载视图层脚本
├── scripts/
│   ├── core/              # ★ 纯逻辑层：只允许 RefCounted，禁止依赖场景树
│   │   ├── tiles.gd       #   格子类型 / 道具类型枚举
│   │   ├── grid.gd        #   方格地图数据结构 + ASCII 互转
│   │   ├── level.gd       #   经典布局生成（硬墙骨架 + 软砖 + 出生点安全区）
│   │   ├── level_data.gd  #   5 关配置 + 「配置 → 可玩地图」组装（藏出口、挑敌人出生点）
│   │   ├── explosion.gd   #   十字爆炸范围计算（纯函数）
│   │   ├── bomb.gd        #   单颗炸弹的引信状态（含遥控炸弹）
│   │   ├── enemy.gd       #   敌人状态与移动节奏
│   │   ├── powerup.gd     #   地图上待拾取的道具
│   │   ├── player_state.gd#   玩家数值与成长（生命/炸弹数/火力/速度/护盾/遥控）
│   │   └── game_session.gd#   单人闯关状态机：爆炸结算、敌人 AI、道具、计时、过关
│   └── main.gd            # ★ 视图层：唯一依赖场景树的脚本
├── tests/
│   ├── test_case.gd       # 极简断言基类
│   ├── test_*.gd          # 各模块测试套件
│   ├── run_tests.gd       # 测试入口（SceneTree 脚本）
│   └── run_tests.sh       # 一键跑测试
└── assets/
    ├── CREDITS.md         # 素材来源与 CC0 许可说明
    ├── art/               # AI 生成插画（标题 / 4 角色选人立绘）
    ├── sprites/           # CC0 像素素材（characters / enemies / fx / items / tiles）
    └── audio/             # 原创 chiptune BGM 与音效（无版权风险，见 CREDITS.md）
```

**分层铁律：**

1. `scripts/core/` 是**纯逻辑层**。只允许继承 `RefCounted`，**禁止**引用 `Node`、场景树、
   `Input`、`Time`、`Engine`、任何渲染 API。所有玩法规则（谁能动、炸多广、谁死谁赢、
   道具怎么生效）**必须**写在这里，并且必须能被 headless 单测覆盖。
2. `scripts/main.gd` 是**视图层**。只做两件事：把 core 的状态画出来、把输入翻译成对 core 的调用。
   **视图层不允许出现任何玩法判定**。
3. 新增玩法的顺序固定为：**先在 core 写逻辑 + 测试 → 再在视图层接渲染**。反过来做一律不接受。
4. 逻辑层不读系统时间、不用随机种子外的全局随机源。需要随机时必须通过传入的
   `RandomNumberGenerator`，保证测试可复现。

## 4. 强制规则（不可协商）

### 规则一：每次代码修改，都必须生成 git commit 版本快照

- 任何对代码、场景、配置的改动，**做完立刻提交**，不允许"改完先放着"，也不允许把多组
  不相关的改动堆进同一个 commit。
- 一个 commit 只做一件事（原子提交）。禁止把"加功能"和"顺手改格式"混在一起。
- commit message 用中文，格式为 `类型: 简述`，类型取以下之一：

  | 类型 | 含义 |
  | --- | --- |
  | `feat` | 新增功能 |
  | `fix` | 修复缺陷 |
  | `test` | 只增改测试 |
  | `refactor` | 重构（行为不变） |
  | `docs` | 文档 |
  | `chore` | 工程/配置/依赖 |

  示例：`feat: 新增炸弹连锁引爆`、`fix: 出生点被软方块堵死`。
- 提交前必须已通过规则二的测试。
- **目的**：每一次改动都可追踪、可回退（`git log` / `git revert` / `git reset`），
  出问题能立刻定位到是哪一次改动引入的。

### 规则二：改代码就必须改测试，交付前必须自己跑测试

- 任何 `scripts/core/` 下的逻辑改动，**必须同步新增或更新 `tests/` 中对应的测试**。
  **只改逻辑不加测试 = 任务未完成，不予交付。**
- 每次提交前，必须自己执行：

  ```bash
  ./tests/run_tests.sh
  ```

  **退出码非 0（存在失败断言）时严禁提交**，必须先修到全绿。
- 修复 bug 的标准流程：**先补一条能复现该 bug 的失败测试 → 再改代码让它变绿**，
  禁止"先改代码，测试以后再说"。
- 新增 core 模块时，必须在 `tests/run_tests.gd` 的 `TEST_SCRIPTS` 里注册对应的测试文件，
  否则测试不会被跑到。
- **目的**：在交付给玩家之前自己发现 bug，而不是把问题留到线上。

### 交付自检清单（每次交付前逐条确认）

- [ ] `./tests/run_tests.sh` 全部通过（退出码 0）
- [ ] 逻辑改动都有对应测试覆盖
- [ ] 改动已按规则一提交为原子 commit
- [ ] 改动已推送到远端仓库备份
- [ ] core 层没有引入任何场景树 / 渲染依赖

## 5. 常用命令

```bash
# 跑测试（交付前必跑）
./tests/run_tests.sh

# 指定 Godot 路径跑测试
GODOT_BIN=/path/to/Godot ./tests/run_tests.sh

# 首次克隆后导入资源（生成 .godot/ 与全局类缓存）
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . --import

# 打开编辑器
/Applications/Godot.app/Contents/MacOS/Godot --path .

# 直接运行游戏
/Applications/Godot.app/Contents/MacOS/Godot --path .
```

## 6. 玩法规格

**地图**

- 俯视 2D 方格迷宫，`15 x 13` 格，由三类格子组成：
  **硬墙**（外框 + 偶数坐标交叉点，不可摧毁且阻挡火焰）、
  **软砖**（可炸毁，炸掉后可能露出道具或隐藏出口）、**通道**（可通行）。
- 玩家出生点固定在左上角 `(1,1)`，开局自动清空出生点及其朝向中心的两格。
- **出口**藏在一块离出生点至少 6 格的软砖下面；软砖没被炸掉前，出口不可见也不可通行。

**操作**

- 一次只能沿上下左右移动一格，不能穿过硬墙、软砖或已有炸弹；可以走出自己刚放的炸弹格。
- 在脚下放置炸弹，引信 `2.5s` 后爆炸。
- 吃到遥控道具后，按 `F` 可手动引爆自己所有的遥控炸弹。

**炸弹与爆炸**

- 爆炸为**十字范围**，半径 = 玩家火力值；硬墙挡住火焰且自身不损，软砖被摧毁并挡住后续扩散。
- 爆炸范围内若有其他炸弹，触发**连锁引爆**。
- 炸弹有携带上限，已放置未爆炸的炸弹占用额度，爆炸后归还。
- 遥控炸弹不会自己倒数，必须手动引爆。

**敌人**

- 每关敌人数量 / 速度由关卡配置决定（2 → 6 只，越往后越快）。
- 敌人逐格移动，只走可通行、没有炸弹、没有其它敌人的格子，大概率保持当前方向。
- 敌人被火焰覆盖即消灭；玩家与敌人同格即受伤。

**伤害与生命**

- 玩家初始 **3 条命**。碰到敌人、或被任意火焰覆盖（**包括自己的炸弹**）即损失 1 条命。
- 阵亡后 1.5s 复活于出生点，并获得 2s 无敌时间。护盾道具期间免疫一切伤害。
- 命扣光即整局结束。复活与过关都会保留成长数值（炸弹数 / 火力 / 速度 / 遥控）。

**强化道具**（软砖被炸毁后有概率掉落）

| 道具 | 效果 | 上限 |
| --- | --- | --- |
| 炸弹 | 可同时携带的炸弹数 +1 | 8 |
| 火焰 | 爆炸半径 +1 | 8 |
| 速度 | 移动速度提升 | 6.0 |
| 护盾 | 一段时间内免疫爆炸与敌人接触 | 8s |
| 遥控 | 获得手动引爆能力（永久） | — |

**时间压力**

- 每关有倒计时（第 1 关 180s，逐关收紧到 140s）。时间耗尽扣 1 条命并重新开始本关。

**过关条件**

- **先清光全部敌人，再站到已显现的出口上**，两者缺一不可；满足后进入下一关。
- 过关按剩余秒数结算时间奖励分。计分：炸软砖 `+10`、击杀敌人 `+200`、拾取道具 `+50`、
  过关时间奖励 `剩余秒数 × 5`。
- 5 关全部通过即通关。

**关卡循环**

进入地图 → 炸墙开路 → 拿强化 → 躲敌人与自己的炸弹 → 清掉敌人 → 找出口 → 下一关。

**手感与音效**

对标参考视频「音乐欢畅 + 打击有重量」的观感，以下规则固定下来：

| 项 | 规格 |
| --- | --- |
| 转向 | 按下方向键**立刻**转身，撞墙也转身（`GameSession.face`），操作零延迟感 |
| 输入缓冲 | 走格冷却期间按下的方向被记住 `0.14s`，冷却一结束立即执行，避免「点了没反应」 |
| 移动 | core 逐格推进，视图层按走格时长做插值追赶，观感连续；撞墙只给 `0.05s` 短冷却，按住方向键贴墙不卡顿 |
| 屏幕震动 | 爆炸 `0.24`、玩家受伤 `0.5` 强度，`0.3s` 内线性衰减；只震动棋盘，HUD 与横幅保持稳定 |
| 打击停顿 | 爆炸冻结 `0.05s`、玩家受伤冻结 `0.14s`，让冲击有分量 |
| 引信滴答 | 场上最急的炸弹进入最后 `1s` 时开始滴答，越接近爆炸越密（`0.09s` → `0.3s`） |
| 飘分 | 击杀 / 拾取道具 / 连锁提示在对应格上方飘出并上浮，`0.9s` 淡出 |
| 连锁奖励 | 一次爆炸清掉 ≥2 只敌人时，额外奖励 `SCORE_ENEMY × (击杀数 - 1)`，鼓励把敌人聚堆再炸 |
| BGM | 标题页与对局各一首原创 chiptune；对局剩余时间 < `30s` 时升调至 `1.14x`，复刻老式炸弹人的「hurry up」压迫感 |
| 音效 | 放炸弹 / 爆炸 / 拾取 / 击杀 / 受伤 / 复活 / 发现出口 / 过关 / 游戏结束 / 超时 各一条，另有脚步与过关 jingle |

**HUD**

- 左侧面板：角色头像、剩余生命、炸弹数 / 上限、火力、速度、遥控状态、总分。
- 面板下方：关数 `n / 5`、剩余时间、暂停提示。

**操作键位**

| 键 | 作用 |
| --- | --- |
| `W A S D` / 方向键 | 四方向移动 |
| `空格` | 放置炸弹 |
| `F` | 手动引爆遥控炸弹 |
| `P` | 暂停 / 继续 |
| `R` | 重开整局 |
| `ESC` | 返回选人界面 |

## 7. 当前实现状态

**已完成**

- core 纯逻辑层全部模块（`tiles` / `grid` / `level` / `level_data` / `explosion` / `bomb`
  / `enemy` / `player_state` / `game_session`）+ **420 项断言全绿**的单元测试
- 单人闯关 5 关循环：随机迷宫、隐藏出口、时间压力、3 条命、通关结算
- 敌人 AI：6 种怪物、按间隔走格、路口偏好转弯、踩到玩家造成伤害
- 炸弹与爆炸：十字火焰、硬墙阻挡、连锁引爆、遥控炸弹手动引爆
- 强化道具：额外炸弹 / 火力 / 速度 / 护盾 / 遥控，共 5 种，软砖按概率掉落
- 视图层接入 CC0 像素素材：4 方向逐帧行走动画、死亡动画、6 种怪物动画、
  爆炸火焰与烟雾、瓦片、道具图标、10 组音效
- 移动手感：core 走格 + 视图层按走格时长插值追赶；转向即时、输入缓冲 `0.14s`、
  撞墙短冷却，操作不再有「点了没反应」的迟滞
- 打击反馈：屏幕震动（爆炸 / 受伤，线性衰减）、打击停顿（`0.05s` / `0.14s`）、
  飘分文字、连锁击杀奖励
- 音频：原创 chiptune BGM 两首（标题 / 对局）+ 引信滴答、脚步、过关 jingle，
  响度统一归一化；剩余时间 < 30s 时 BGM 升调至 1.14x
- 场景状态机：标题 → 选人（4 角色）→ 对局 → 过关 / 游戏结束，切场景同步切 BGM
- 仿参考截图的左侧 HUD 面板：头像、生命、炸弹数、火力、速度、遥控、分数、
  关数、剩余时间
- AI 生成美术：标题背景、4 张角色立绘、地板纹理（详见 `assets/CREDITS.md`）

**已知现象（非缺陷）**

- 用 `--quit-after` 退出时可能打印 `ObjectDB instances leaked at exit` 与
  `AudioStreamWAV` 资源仍在使用。原因是 Godot 4.3 的 `AudioStreamPlayer.stop()`
  要等下一次音频混音才真正释放 playback，退出后不再有混音，属引擎侧行为；
  `main.gd` 已在 `_exit_tree()` 里做了正确清理，不影响游戏运行。

**待办**

- 导出 Web / macOS 可分发版本
- 更多关卡主题与 BGM 变奏
- 敌人种类行为差异化（当前 6 种共用一套寻路策略）
- 更多道具（参考原作：拳击手套推炸弹、弹跳器）
- 存档与最高分记录

## 8. 明确禁止

- 禁止在 `scripts/core/` 中引用场景树、`Node`、`Input`、`Time`、渲染 API。
- 禁止未跑测试就提交，禁止带着失败断言提交。
- 禁止把不相关的改动塞进同一个 commit。
- 禁止提交 `.godot/`、构建产物等生成文件（已写入 `.gitignore`）。
- 禁止为了让测试变绿而删除或弱化已有断言（除非该断言本身被证明是错的，且在同一
  commit 中说明原因）。
